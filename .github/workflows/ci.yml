name: autostream-docs CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  docs:
    runs-on: blacksmith-2vcpu-ubuntu-2404
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci
      - run: npm run docs:check
      - run: npm run docs:build
